# 🚀 Настройка Cloudflare Pages для IQ Test

Простое руководство по деплою статического сайта на Cloudflare Pages.

---

## 📋 Быстрый старт (Ручная настройка)

**Время: 3-5 минут**

### Шаг 1: Подготовка

Убедитесь, что:
- ✅ Код запушен в GitHub репозиторий: `https://github.com/gorelikspb/iqtest`
- ✅ Все файлы находятся в папке `public/` (уже создана)
- ✅ У вас есть аккаунт Cloudflare (бесплатный план работает)

### Шаг 2: Откройте Cloudflare Dashboard

1. Перейдите: https://dash.cloudflare.com/
2. Войдите в аккаунт
3. В левом меню найдите **"Workers & Pages"** → **"Pages"**
4. Или перейдите напрямую: https://dash.cloudflare.com/pages

### Шаг 3: Создайте проект Pages

1. Нажмите **"Create a project"**
2. Выберите **"Connect to Git"**

### Шаг 4: Подключите GitHub

1. Нажмите **"Connect GitHub account"** (если еще не подключен)
2. Авторизуйте Cloudflare доступ к GitHub
3. Выберите репозиторий: `gorelikspb/iqtest`
4. Нажмите **"Begin setup"**

### Шаг 5: Настройте проект

**Критически важные настройки:**

| Параметр | Значение | Примечание |
|----------|----------|------------|
| **Project name** | `iqtest` | Если имя занято, выберите другое (например, `iq-test-online`) |
| **Production branch** | `main` | Ветка с кодом |
| **Framework preset** | **"None"** | Статический сайт без фреймворка |
| **Build command** | **ОСТАВЬТЕ ПУСТЫМ** | Нет build процесса |
| **Build output directory** | `public` | ⚠️ **ОБЯЗАТЕЛЬНО!** Папка с файлами |
| **Root directory** | оставьте пустым | Или `/` |

### Шаг 6: Деплой

1. Нажмите **"Save and Deploy"**
2. Дождитесь завершения деплоя (обычно 1-2 минуты)
3. После успешного деплоя вы увидите URL вашего сайта

### Шаг 7: Получите URL

После деплоя сайт будет доступен по адресу:
```
https://iqtest.pages.dev
```

**Важно:** Если имя `iqtest` занято, Cloudflare предложит альтернативное имя (например, `iqtest-xyz123`). Запишите это имя!

---

## ✅ Проверка после деплоя

### 1. Проверьте главную страницу

Откройте URL сайта в браузере и убедитесь, что:
- ✅ Страница загружается
- ✅ Стили применяются корректно
- ✅ JavaScript работает
- ✅ Тест запускается

### 2. Проверьте функциональность

- ✅ Можно пройти тест
- ✅ Результаты отображаются
- ✅ Форма сбора контактов работает
- ✅ Кнопки поделиться работают
- ✅ Страница расширенных тестов открывается

### 3. Проверьте отправку email

- ✅ Заполните форму после прохождения теста
- ✅ Проверьте email `gorelikgo@gmail.com` - должно прийти письмо

---

## 🔄 Автоматический деплой

После первой настройки, каждый push в ветку `main` автоматически запустит новый деплой.

**Как это работает:**
1. Вы делаете `git push` в GitHub
2. Cloudflare Pages автоматически обнаруживает изменения
3. Запускается новый деплой (обычно 1-2 минуты)
4. Сайт обновляется автоматически

**Проверка статуса деплоя:**
- Откройте dashboard: https://dash.cloudflare.com/pages
- Выберите проект `iqtest`
- Во вкладке **"Deployments"** вы увидите историю деплоев

---

## 🛠️ Troubleshooting

### Проблема: Сайт не доступен

**Возможные причины:**
- Деплой еще не завершился (подождите 1-2 минуты)
- Неправильно указан Build output directory

**Решение:**
1. Проверьте статус деплоя в dashboard
2. Убедитесь, что Build output directory: `public`

### Проблема: 404 ошибки на страницах

**Возможные причины:**
- Неправильный Build output directory
- Файлы не закоммичены в Git

**Решение:**
1. Убедитесь, что Build output directory: `public`
2. Проверьте, что все файлы закоммичены: `git status`
3. Сделайте push: `git push`

### Проблема: Стили/скрипты не загружаются

**Возможные причины:**
- Неправильные пути к файлам
- Файлы не в папке `public/`

**Решение:**
1. Проверьте, что пути относительные (без `/` в начале):
   - ✅ Правильно: `href="style.css"`
   - ❌ Неправильно: `href="/style.css"`
2. Убедитесь, что `style.css`, `script.js`, `full-tests.js` в папке `public/`

### Проблема: Деплой падает с ошибкой

**Возможные причины:**
- Ошибка в коде
- Неправильные настройки build

**Решение:**
1. Откройте логи деплоя в dashboard
2. Проверьте ошибки в консоли
3. Убедитесь, что Build command пустой (для статического сайта)

### Проблема: Имя проекта занято

**Решение:**
- Выберите другое имя (например, `iq-test-online`, `iqtest-online`, `quick-iq-test`)

---

## 📚 Полезные ссылки

- **Cloudflare Pages Dashboard**: https://dash.cloudflare.com/pages
- **Документация Cloudflare Pages**: https://developers.cloudflare.com/pages/
- **GitHub репозиторий**: https://github.com/gorelikspb/iqtest

---

## 📝 Чеклист деплоя

Перед деплоем убедитесь:

- [ ] Код запушен в GitHub
- [ ] Все файлы в папке `public/` (index.html, style.css, script.js, full-tests.html, full-tests.js)
- [ ] Build output directory: `public`
- [ ] Build command: пустой
- [ ] Framework preset: None
- [ ] GitHub репозиторий подключен (`gorelikspb/iqtest`)
- [ ] Production branch: `main`

После деплоя проверьте:

- [ ] Сайт доступен по URL (`https://iqtest.pages.dev`)
- [ ] Страница загружается
- [ ] Стили применяются
- [ ] JavaScript работает
- [ ] Тест можно пройти
- [ ] Форма отправляет данные на Worker
- [ ] Email приходит на `gorelikgo@gmail.com`

---

**Готово!** Ваш сайт должен быть доступен и работать корректно. 🎉

